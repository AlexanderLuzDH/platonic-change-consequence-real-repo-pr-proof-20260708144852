# Prospective beta v1 amendments

No amendments have been made.

After the first eligible PR is assigned, append amendments rather than rewriting history. Each entry must include:

- UTC timestamp;
- author;
- reason;
- exact files or definitions changed;
- affected record IDs;
- whether a primary estimand, treatment, sampling probability, or stopping rule changed;
- whether the instrumentation run-in or main trial restarts;
- commit and content digests.
